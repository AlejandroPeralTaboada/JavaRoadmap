package com.alexperal.training.java.lists.l02;

import com.alexperal.training.java.lists.l01.MinimalIntList;

/**
 * Splits the array in two and sorts each part applying the algorithm again
 *
 * Under a threshold of size 12 of an array it uses insertion sort instead
 *
 * Then uses the two sorted arrays to merge them into the final one
 *
 * https://en.wikipedia.org/wiki/Merge_sort
 */
public class Sorting05EnhancedMergeSort implements Sorting00Sorter{


    @Override
    public MinimalIntList sort(MinimalIntList list) {

        return list;
    }

}
